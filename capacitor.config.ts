import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abueslam.contractorbook',
  appName: 'دفتر المقاول',
  webDir: 'dist'
};

export default config;
